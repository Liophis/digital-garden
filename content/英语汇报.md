
### 第一页

its my time to share my work on this paper attention is all you need！My part focus on two critical parts of the paper : section 4 whitch explain why they use the sefl attention and section 5 whitch detials on how we trained it . 

### 第二页
let s start with the section 4. why did the author choose sefl attention over the traditionnal recurrent neutral networks shortened as RNNs or convolutionnal nns. They evaluated this based on three critera below. 
1. is 看ppt。Self attention requires less computation per layer , makng it faster
2. is 看ppt unlike rnns which process words sequentially sefl attention processes everything at once, make the computation highly parallelizabel.
3. and the most importly is path length
 to learn long - range dependencies ,the signal path mush be short . 
In Self-Attention, every word connects directly to every other word, making the path length just O(1), whereas in RNNs it is O(n)."

As side benefit, self-attention could yield more interpretable models.

### 第三页

**Table 1** proves the theoretical efficiency. You can see the Maximum Path Length for Self-Attention is constant at **O(1)**. This confirms why it captures long-range dependencies so well.

### 第四页
Moving on to Section 5, let's look at the **Training Recipe**. We can break it down into four components.

1. We used the massive WMT 2014 datasets with Byte-Pair Encoding 
2. Second, **Hardware**.  The base model was trained on 8 P100 GPUs in just **12 hours**, which is a fraction of the cost of previous models but have the performance as good as the state of the art .
3. the **Optimizer**. We used Adam with a special **'Warm-up' schedule** to stabilize early training.
4. Finally, **Regularization**. We employed **Residual Dropout** and **Label Smoothing**. Although label smoothing hurts perplexity, it significantly improves the final accuracy.

### 第五页

Beyond the content, we also analyzed the **Logical Structure** of these two sections."

- **(Left Side)**  
    "Section 4 follows a **Comparative Argument**. The authors first established criteria  and then theoretically compared models against them to logically **prove** the superiority of Self-Attention.
    
- **(Right Side)**  
    "Section 5, however, follows a **Descriptive Pipeline**. It is written in a strict time order—Data, Hardware, Optimizer, Regularization—to ensure the experiment is scientifically **reproducible**.

### Last but not least
"For vocabulary, we found precise terms like **'Desiderata'** (meaning essential needs) and **'Employ'** (instead of 'use'). We also highlighted **'In terms of'** to strictly limit the scope of comparison.

formal contrast 
**Trade-off structure**: 'This hurts [A]... but improves [B]'. This is excellent for objectively discussing limitations.
phrases like **'Motivating our use of...'** allow us to professionally introduce our research reasons