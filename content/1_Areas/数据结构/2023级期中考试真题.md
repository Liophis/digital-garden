
## 判断题

- [ ] 在存储数据时，只需要存储各数据的元素的值和数据元素的类型 ——> 还需要存储数据元素之间的逻辑关系。
- [ ] 设计表达式括号匹配算法时，会用到队列结构。 ——> 使用的是栈。
- [ ] 某二叉树中序序列的第一个结点不是叶结点，则该结点一定是其后序遍历的第一个结点。
- [ ] 采用三元组存储矩阵，仍然可以进行随机存取。
- [x] 消除递归不一定要使用栈。 ——> 尾递归转换成一个简单的循环。